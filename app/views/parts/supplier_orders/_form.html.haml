= form_for @parts_supplier_order do |f|
  - if @parts_supplier_order.errors.any?
    #error_explanation.alert.alert-danger.alert-dismissible.fade.show.p-3.m-3
      %h2= "#{pluralize(@parts_supplier_order.errors.count, "error")} prohibited this parts_supplier_order from being saved:"
      %button{type: 'button', class: 'close', 'data-dismiss': 'alert', 'arial-label': 'Close'}
        %span{'aria-hidden': 'true'} x
      %ul
        - @parts_supplier_order.errors.full_messages.each do |message|
          %li= message

  .form-group
    = f.label :employee_name
    = f.text_field :employee_name, class: 'form-control'
  .form-group
    = f.label :date
    = f.text_field :date, class: 'form-control'
  .form-group
    = f.label :supplier_tradename
    = f.text_field :supplier_tradename, class: 'form-control'
  .form-group
    = f.label :status
    = f.text_field :status, class: 'form-control'
  .form-group
    = f.label :delivery_date
    = f.text_field :delivery_date, class: 'form-control'
  .form-group
    = f.label :article
    = f.text_field :article, class: 'form-control'
  .form-group
    = f.label :price
    = f.text_field :price, class: 'form-control'
  .form-group
    = f.label :amount
    = f.text_field :amount, class: 'form-control'
  .form-group
    = f.label :total
    = f.text_field :total, class: 'form-control'
  .actions
    = f.submit 'Save', class: 'btn btn-primary'

= form_for @sales_employee do |f|
  - if @sales_employee.errors.any?
    #error_explanation.alert.alert-danger.alert-dismissible.fade.show.p-3.m-3{role: 'alert'}
      %button{type: 'button', class: 'close', 'data-dismiss': 'alert', 'aria-label': 'Close'}
        %span{"aria-hidden": "true"} ×
      %h2= "#{pluralize(@sales_employee.errors.count, "error")} prohibited this sales employee from being saved:"
      %ul
        - @sales_employee.errors.full_messages.each do |message|
          %li= message

  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'
  .form-group
    = f.label :last_name
    = f.text_field :last_name, class: 'form-control'
  .form-group
    = f.label :phone
    = f.text_field :phone, class: 'form-control'
  .form-group
    = f.label :email
    = f.text_field :email, class: 'form-control'
  .actions
    = f.submit 'Save', class: 'btn btn-primary'

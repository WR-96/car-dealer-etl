= form_for @sales_bill do |f|
  - if @sales_bill.errors.any?
    #error_explanation.alert.alert-danger.alert-dismissible.fade.show.p-3.m-3{ role: 'alert' }
      %h2= "#{pluralize(@sales_bill.errors.count, "error")} prohibited this sales_bill from being saved:"
      %button{type: 'button', class: 'close', 'data-dismiss': 'alert', 'aria-label': 'Close'}
        %span{"aria-hidden": "true"} ×
      %ul
        - @sales_bill.errors.full_messages.each do |message|
          %li= message

  .form-group
    = f.label :client_name
    = f.text_field :client_name, class: 'form-control'
  .form-group
    = f.label :rfc
    = f.text_field :rfc, class: 'form-control'
  .form-group
    = f.label :date
    = f.text_field :date, class: 'form-control'
  .form-group
    = f.label :sub_total
    = f.text_field :sub_total, class: 'form-control'
  .form-group
    = f.label :tax_percentage
    = f.text_field :tax_percentage, class: 'form-control'
  .form-group
    = f.label :tax
    = f.text_field :tax, class: 'form-control'
  .form-group
    = f.label :total
    = f.text_field :total, class: 'form-control'
  .actions
    = f.submit 'Save', class: 'btn btn-primary'

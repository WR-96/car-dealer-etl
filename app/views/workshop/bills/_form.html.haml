= form_for @workshop_bill do |f|
  - if @workshop_bill.errors.any?
    #error_explanation
      %h2= "#{pluralize(@workshop_bill.errors.count, "error")} prohibited this workshop_bill from being saved:"
      %ul
        - @workshop_bill.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :date
    = f.text_field :date
  .field
    = f.label :client_name
    = f.text_field :client_name
  .field
    = f.label :rfc
    = f.text_field :rfc
  .field
    = f.label :sub_total
    = f.text_field :sub_total
  .field
    = f.label :tax_percentage
    = f.text_field :tax_percentage
  .field
    = f.label :tax_amount
    = f.text_field :tax_amount
  .field
    = f.label :total
    = f.text_field :total
  .actions
    = f.submit 'Save'
